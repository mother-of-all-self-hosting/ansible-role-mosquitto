# SPDX-FileCopyrightText: 2023 Julian-Samuel Gebühr
#
# SPDX-License-Identifier: AGPL-3.0-or-later

---
- name: Perform Mosquitto installation tasks
  when: mosquitto_enabled | bool
  tags:
    - setup-all
    - setup-mosquitto
    - install-all
    - install-mosquitto
  block:
    - name: Validate Mosquitto configuration
      ansible.builtin.include_tasks: "{{ role_path }}/tasks/validate_config.yml"
    - name: Install Mosquitto
      ansible.builtin.include_tasks: "{{ role_path }}/tasks/install.yml"

- name: Perform Mosquitto user creation tasks
  when: mosquitto_enabled | bool
  tags:
    - create-user-mosquitto
    - mosquitto-add-user
  block:
    - name: Add a Mosquitto user account
      ansible.builtin.include_tasks: "{{ role_path }}/tasks/user_create.yml"

- name: Perform Mosquitto uninstallation tasks
  when: not mosquitto_enabled | bool
  tags:
    - setup-all
    - setup-mosquitto
  block:
    - name: Uninstall Mosquitto
      ansible.builtin.include_tasks: "{{ role_path }}/tasks/uninstall.yml"
